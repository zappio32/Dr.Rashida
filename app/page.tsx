import Link from 'next/link';
import { prisma } from '@/lib/prisma';

export const dynamic = 'force-dynamic';

const fallbackServices = [
  { id: 'online-consultation', name: 'Online Ayurvedic Consultation', description: 'Personalized guidance from the comfort of home.', durationMin: 30, fee: 0 },
  { id: 'clinic-consultation', name: 'Clinic Consultation', description: 'An in-person consultation at the clinic.', durationMin: 30, fee: 0 }
];

export default async function Home() {
  let doctor = null;
  let services = fallbackServices;
  let demo: { value: unknown } | null = null;
  let databaseUnavailable = false;
  try {
    [doctor, services, demo] = await Promise.all([
      prisma.doctorProfile.findFirst(),
      prisma.service.findMany({ where: { active: true }, orderBy: { createdAt: 'asc' } }),
      prisma.systemSetting.findUnique({ where: { key: 'DEMO_MODE' } })
    ]);
  } catch {
    databaseUnavailable = true;
  }
  const name = doctor?.displayName || 'Dr. Rashida Ahmad';
  return <div><header className="header"><div className="container nav"><Link className="brand" href="/"><span className="mark">R</span>{name}</Link><nav className="links"><Link href="#about">About</Link><Link href="#services">Services</Link><Link href="#how">How it works</Link><Link href="#contact">Contact</Link></nav><div className="actions"><Link className="btn quiet" href="/login">Sign in</Link><Link className="btn primary" href="/book">Book appointment</Link></div></div></header>{databaseUnavailable && <div className="demo">The public site is online, but appointment booking is temporarily unavailable while the database connection is being configured.</div>}{demo?.value === true && <div className="demo">Demo/Test mode is active. Payment and notifications use configured sandbox providers.</div>}<main><section className="hero"><div className="container hero-grid"><div><div className="eyebrow">Ayurvedic care, made personal</div><h1>Your journey towards better health, naturally.</h1><p className="lede">Book a personalized consultation with Dr. Rashida Ahmad and receive professional Ayurvedic guidance through online or clinic consultation.</p><div className="hero-actions"><Link className="btn primary" href="/book">Book appointment</Link><Link className="btn soft" href="/book?type=ONLINE">Consult online</Link></div><div className="trust"><span>Personalized consultation</span><span>Easy online booking</span><span>Online & clinic consultation</span><span>Secure patient information</span></div></div><div className="portrait"><div className="portrait-note"><b>{name}</b><br/><span className="muted">Ayurvedic Doctor</span></div></div></div></section><section id="about" className="band"><div className="container two-col"><div className="panel"><div className="eyebrow" style={{color:'#d5e4d6'}}>Meet your doctor</div><h2 style={{marginTop:18}}>Care that listens first.</h2><p>{doctor?.bio || 'Thoughtful consultations shaped around your story, routines, and wellbeing goals.'}</p></div><div><div className="eyebrow">A considered approach</div><h2 style={{margin:'16px 0'}}>{name}</h2><p className="lede">{doctor?.specialization || 'Ayurvedic Doctor'} offering calm, practical guidance for everyday health and wellness.</p><p className="disclaimer">Online information and consultations do not replace emergency medical care. For urgent symptoms, contact local emergency services.</p></div></div></section><section id="services"><div className="container"><div className="section-head"><div><div className="eyebrow">Choose your starting point</div><h2 style={{marginTop:14}}>Services for your rhythm.</h2></div><p>Every service is designed to give you a clear next step, with enough time for a genuine conversation.</p></div><div className="cards">{services.map(service => <article className="card" key={service.id}><div className="eyebrow">{service.durationMin} minutes</div><h3 style={{margin:'20px 0 8px'}}>{service.name}</h3><p>{service.description}</p><div className="price">{service.fee ? `₹${service.fee.toLocaleString('en-IN')}` : 'Fee configured by clinic'}</div></article>)}</div></div></section><section id="how" className="band"><div className="container"><div className="eyebrow">Simple from the first click</div><h2 style={{margin:'14px 0 38px'}}>A quieter way to book.</h2><div className="steps"><div className="step"><b>01</b><strong>Select a service</strong><p>Choose online or clinic consultation and find the right session.</p></div><div className="step"><b>02</b><strong>Pick a time</strong><p>See availability calculated from the doctor's schedule.</p></div><div className="step"><b>03</b><strong>Share your details</strong><p>Tell us what you would like to discuss before your appointment.</p></div><div className="step"><b>04</b><strong>Meet with care</strong><p>Receive confirmation and join your consultation when it is time.</p></div></div></div></section><section id="contact"><div className="container section-head"><div><div className="eyebrow">Stay connected</div><h2 style={{marginTop:14}}>Questions before booking?</h2></div><p>Reach the clinic through the contact details configured by your administrator.</p></div></section></main><footer className="footer"><div className="container footer-row"><b>{name}</b><span>Ayurvedic Doctor · Online & Clinic Consultation</span><span>Information here does not replace emergency medical care.</span></div></footer></div>;
}
